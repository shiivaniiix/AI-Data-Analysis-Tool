import type { Config } from "tailwindcss";

/**
 * Tailwind v4 resolves content via `src/app/globals.css` (`@import "tailwindcss" source(...)`).
 * This file keeps IntelliSense / tooling happy and can hold future theme extensions.
 */
export default {} satisfies Config;
