/** localStorage keys for session — single source of truth for the frontend. */
export const STORAGE_KEYS = {
  TOKEN: "datachat_token",
  USER_EMAIL: "datachat_user_email",
  USERNAME: "datachat_username",
  NEW_CHAT_ID: "datachat_new_chat_id",
} as const;
